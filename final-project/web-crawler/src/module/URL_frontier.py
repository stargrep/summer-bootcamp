# this function should save all url links, and remove duplicate ones
def save_url_links(url_links):

# this function should return a url link, which is used for HTML_fetcher to access & download web page.
def get_new_url():